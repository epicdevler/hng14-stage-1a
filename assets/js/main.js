import { handleActionButtons, handleCheckChange, handleStatusOptionChange, propagateTaskDataToUI, updateTimeRemaining } from "./ui-manage.js";





propagateTaskDataToUI()


setInterval(updateTimeRemaining, 60000);

handleStatusOptionChange()

handleActionButtons()

handleCheckChange()